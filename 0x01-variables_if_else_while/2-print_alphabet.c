#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - get the signe of numbre
 *
 * @tolower - get the
 *
 * Return: 0
 */
int main(void)
{
	for (int x = 'A'; x <= 'Z'; x++)
{
	putchar(tolower(x));
}
	return (0);
}
