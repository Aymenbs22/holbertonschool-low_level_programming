#include "main.h"
/**
 * _isalpha - function that checks for lowercase character.
 * @c:first letter
 * Return: 0
 */
int _isalpha(int c)
{
	if (c <= 'z' && c >= 'A')
{
	return (1);
	}
	return (0);
}
