include "main.h"
/**
 * _islower - function that checks for lowercase character.
 * @c:first letter
 * Return: 0
 */
int _abs(int n)
{
   if (n < 0)
    {
            return (-n);
    }
    else
    {
            return (n);
    }
    return(0);
}
