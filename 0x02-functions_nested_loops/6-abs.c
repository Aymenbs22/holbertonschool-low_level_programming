#include "main.h"
/**
 * _abs - function that checks for lowercase character.
 * @n:first letter
 * Return: 0
 * @n:similar function
 */
int _abs(int n)
{
	if (n < 0)
{
	return (-n);
}
	else
{
	return (n);
}
	return (0);
}
