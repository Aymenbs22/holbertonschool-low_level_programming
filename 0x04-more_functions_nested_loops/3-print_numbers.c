#include "main.h"
/**
 * print_numbers - get the signe of numbre
 *
 * @tolower - get the
 *
 * Return: 0
 */
void print_numbers(void)
{
	int a = 0;
while (a < 10)
{
	_putchar(a + 48);
	a++;
}
	_putchar('\n');
	return (0);
}
